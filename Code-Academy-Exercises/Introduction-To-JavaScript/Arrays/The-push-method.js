const chores = ['wash dishes', 'do laundry', 'take out trash'];

chores.push('cook dinner', 'mop floor');

console.log(chores);


// push is a method of adding data to an array, it places the new entry at the end of the array, after the current last entry.
